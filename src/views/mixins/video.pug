mixin video(video)
    div     
        h4 
            a(href=`/video/${video.id}`)=video.title
        ul  
            li #{video.rating}/5.
            li #{video.comments} comments.
            li #{video.views}.
